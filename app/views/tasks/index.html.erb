  <nav>
    <div class="nav-wrapper blue-grey lighten-1">
      <ul class="right hide-on-med-and-down blue-grey lighten-1">
        <li><%= link_to 'New Task', new_task_path, class: "waves-effect waves-light btn blue-grey darken-1" %></li>
      </ul>
    </div>
  </nav>
<h4><%= "Account: #{current_user.email}" %></h4>
<hr>
  <% @tasks.each do |task|%>
  <div class="row">
    <div class="col s12 m4">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"><%= task.title %></span>
          <p><%= task.description %></p>
        </div>
        <div class="card-action">
          <%= link_to "Delete", task_path(task), method: :delete %>
          <%= link_to "Edit", edit_task_path(task) %>
        </div>
      </div>
    </div>
  </div>
   <% end %>
            
  
<%= button_to "Log out", destroy_user_session_path, method: :delete, class: "btn waves-effect waves-light blue-grey darken-1" %>
